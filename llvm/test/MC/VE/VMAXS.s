# RUN: llvm-mc -triple ve-unknown-unknown --show-encoding %s | FileCheck %s

# CHECK: vrmaxs.w.fst.sx %v11, %v12
# CHECK: encoding: [0x00,0x00,0x0c,0x0b,0x00,0x00,0x00,0xbb]
vrmaxs.w.fst.sx %v11, %v12

# CHECK: vrmaxs.w.fst.sx %v11, %vix, %vm11
# CHECK: encoding: [0x00,0x00,0xff,0x0b,0x00,0x00,0x0b,0xbb]
vrmaxs.w.fst.sx %v11, %vix, %vm11

# CHECK: vrmaxs.w.lst.sx %vix, %v22, %vm15
# CHECK: encoding: [0x00,0x00,0x16,0xff,0x00,0x00,0x2f,0xbb]
vrmaxs.w.lst.sx %vix, %v22, %vm15

# CHECK: vrmaxs.w.lst.zx %v63, %v60, %vm2
# CHECK: encoding: [0x00,0x00,0x3c,0x3f,0x00,0x00,0x62,0xbb]
vrmaxs.w.lst.zx %v63, %v60, %vm2

# CHECK: vrmaxs.w.fst.zx %vix, %vix
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x40,0xbb]
vrmaxs.w.fst.zx %vix, %vix, %vm0

# CHECK: vrmaxs.w.lst.zx %vix, %vix, %vm2
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x62,0xbb]
vrmaxs.w.lst.zx %vix, %vix, %vm2

# CHECK: vrmins.w.fst.sx %v11, %v12
# CHECK: encoding: [0x00,0x00,0x0c,0x0b,0x00,0x00,0x10,0xbb]
vrmins.w.fst.sx %v11, %v12

# CHECK: vrmins.w.fst.sx %v11, %vix, %vm11
# CHECK: encoding: [0x00,0x00,0xff,0x0b,0x00,0x00,0x1b,0xbb]
vrmins.w.fst.sx %v11, %vix, %vm11

# CHECK: vrmins.w.lst.sx %vix, %v22, %vm15
# CHECK: encoding: [0x00,0x00,0x16,0xff,0x00,0x00,0x3f,0xbb]
vrmins.w.lst.sx %vix, %v22, %vm15

# CHECK: vrmins.w.lst.zx %v63, %v60, %vm2
# CHECK: encoding: [0x00,0x00,0x3c,0x3f,0x00,0x00,0x72,0xbb]
vrmins.w.lst.zx %v63, %v60, %vm2

# CHECK: vrmins.w.fst.zx %vix, %vix
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x50,0xbb]
vrmins.w.fst.zx %vix, %vix, %vm0

# CHECK: vrmins.w.lst.zx %vix, %vix, %vm2
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x72,0xbb]
vrmins.w.lst.zx %vix, %vix, %vm2
