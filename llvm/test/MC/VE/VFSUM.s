# RUN: llvm-mc -triple ve-unknown-unknown --show-encoding %s | FileCheck %s

# CHECK: vfsum.d %v11, %v12
# CHECK: encoding: [0x00,0x00,0x0c,0x0b,0x00,0x00,0x00,0xec]
vfsum.d %v11, %v12

# CHECK: vfsum.d %v11, %vix, %vm11
# CHECK: encoding: [0x00,0x00,0xff,0x0b,0x00,0x00,0x0b,0xec]
vfsum.d %v11, %vix, %vm11

# CHECK: vfsum.d %vix, %v22, %vm15
# CHECK: encoding: [0x00,0x00,0x16,0xff,0x00,0x00,0x0f,0xec]
vfsum.d %vix, %v22, %vm15

# CHECK: vfsum.s %v63, %v60, %vm2
# CHECK: encoding: [0x00,0x00,0x3c,0x3f,0x00,0x00,0x82,0xec]
vfsum.s %v63, %v60, %vm2

# CHECK: vfsum.s %vix, %vix
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x80,0xec]
vfsum.s %vix, %vix, %vm0

# CHECK: vfsum.s %vix, %vix, %vm2
# CHECK: encoding: [0x00,0x00,0xff,0xff,0x00,0x00,0x82,0xec]
vfsum.s %vix, %vix, %vm2
